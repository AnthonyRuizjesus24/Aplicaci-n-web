---
---

<section class="featured-packages">
  <h2>Paquetes Destacados</h2>
  <div class="packages">
    <div class="package">
      <img src="/fondo.jpg" alt="Paquete Aventura" />
      <h3>Paquete Aventura</h3>
      <p>Explora lo desconocido con nuestra oferta de aventura.</p>
      <a href="/packages" class="cta-button">Explora Paquetes</a>
    </div>
    <!-- Más paquetes aquí -->
  </div>
</section>

<style>
.featured-packages {
    background: transparent;
    padding: 4rem 2rem;
    text-align: center;
    color: white;
    font-family: 'Arial', sans-serif;
}

.featured-packages h2 {
    font-size: 2.5rem;
    margin-bottom: 2rem;
    font-weight: bold;
    animation: fadeInDown 1s ease-out;
}

.packages {
    display: flex;
    flex-wrap: wrap;
    gap: 2rem;
    justify-content: center;
}

.package {
    background: white;
    color: #333;
    border-radius: 10px;
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
    overflow: hidden;
    transition: transform 0.3s, box-shadow 0.3s;
    max-width: 300px;
    width: 100%;
    margin: 1rem;
}

.package:hover {
    transform: translateY(-10px);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
}

.package img {
    width: 100%;
    height: 200px;
    object-fit: cover;
}

.package h3 {
    font-size: 1.8rem;
    margin: 1rem 0;
    color: #ff5722;
}

.package p {
    font-size: 1rem;
    padding: 0 1rem;
    color: #555;
}

.cta-button {
    padding: 10px 20px;
    font-size: 1rem;
    color: white;
    background-color: #ff5722;
    border: none;
    border-radius: 5px;
    text-transform: uppercase;
    cursor: pointer;
    text-decoration: none;
    transition: background-color 0.3s ease, transform 0.2s ease;
    display: inline-block;
    margin: 1rem;
}

.cta-button:hover {
    background-color: #e64a19;
    transform: translateY(-2px);
}

.cta-button:active {
    background-color: #d84315;
    transform: translateY(0);
}

@keyframes fadeInDown {
    from {
        opacity: 0;
        transform: translateY(-20px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

@media (max-width: 768px) {
    .packages {
        flex-direction: column;
        align-items: center;
    }

    .package {
        max-width: 90%;
    }
}

@media (max-width: 480px) {
    .featured-packages h2 {
        font-size: 2rem;
    }

    .package h3 {
        font-size: 1.5rem;
    }

    .package p {
        font-size: 0.9rem;
    }
}
</style>
