---
// Puedes importar componentes si los tienes
import Header from '../component/Header.astro';
import Footer from '../component/Footer.astro';
import '../styles/global.css'; // Asegúrate de tener tu archivo CSS
import '../styles/paquetes.css'
import GaleriaD from '../component/destinos/Formulario.astro';
import Formulario from '../component/destinos/Formulario.astro';
import Whatsapp from '../component/Whatsapp.astro';
const pageTitle = "Paquetes";
---

<html lang="es">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>{pageTitle}</title>
  </head>
  <body>
    <Header />
    <main>
    <!-- Introducción de Paquetes -->
<section class="intro-packages">
  <h1>Explora Nuestros Paquetes</h1>
  <p>Encuentra el paquete de viaje perfecto que se ajuste a tus necesidades y presupuesto. Ya sea una escapada romántica, una aventura emocionante o una relajante estancia en la playa, tenemos algo para todos.</p>
</section>

<!-- Lista de Paquetes -->
<section class="packages-list">
  <h2>Paquetes Disponibles</h2>
  <div class="package-card">
    <img src="/selva.jpg" alt="Paquete Aventura en la Selva" />
    <div class="package-info">
      <h3>Paquete Aventura en la Selva</h3>
      <p>Embárcate en una emocionante aventura en la selva tropical con guías expertos.</p>
      <a href="/paquete/aventura-selva" class="btn-more">Más Información</a>
    </div>
  </div>
  <div class="package-card">
    <img src="/playa.jpg" alt="Escapada a la Playa" />
    <div class="package-info">
      <h3>Escapada a la Playa</h3>
      <p>Disfruta de unas vacaciones relajantes en la playa con todo incluido.</p>
      <a href="/paquete/escapada-playa" class="btn-more">Más Información</a>
    </div>
  </div>
</section>

<!-- Filtros de Paquete -->
<section class="package-filters">
  <h2>Filtra tu Paquete</h2>
  <form action="/search" method="get">
    <div class="form-group">
      <label for="type">Tipo:</label>
      <select id="type" name="type">
        <option value="adventure">Aventura</option>
        <option value="beach">Playa</option>
        <option value="romantic">Romántico</option>
      </select>
    </div>
  
    <div class="form-group">
      <label for="duration">Duración:</label>
      <select id="duration" name="duration">
        <option value="3days">3 Días</option>
        <option value="7days">7 Días</option>
        <option value="14days">14 Días</option>
      </select>
    </div>
  
    <div class="form-group">
      <label for="price">Precio:</label>
      <input type="text" id="price" name="price" placeholder="Ej. 500-1000" />
    </div>
  
    <button type="submit">Buscar</button>
  </form>
</section>


<Whatsapp />

      <Formulario/>
    </main>
    <Footer />
  </body>
</html>

<script>
  document.addEventListener('DOMContentLoaded', () => {
  const form = document.querySelector('.package-filters form');
  const priceInput = document.getElementById('price') as HTMLInputElement;

  form.addEventListener('submit', (event) => {
    const priceValue = priceInput.value.trim();
    if (!/^\d+-\d+$/.test(priceValue)) {
      event.preventDefault();
      alert('Por favor, ingresa un rango de precios válido. Ejemplo: 500-1000');
    }
  });
});

</script>
